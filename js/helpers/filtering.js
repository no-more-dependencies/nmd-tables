//@ts-check

/**
 * @typedef {"=" | "!=" | "<" | "<=" | ">" | ">=" | "~" | "!~" | "in" | "!in"} FilterOperators
 */

/**
 * @typedef {Object} Filter
 * @property {string} key
 * @property {FilterOperators} operator
 * @property {any} value
 */

export default function filter(){
	// Dummy
}